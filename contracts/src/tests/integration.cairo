mod tests {
  use mancala::models::player::PlayerSide;
  use mancala::systems::actions::{IActionsDispatcher, IActionsDispatcherTrait};
  use dojo::world::IWorldDispatcher;

  #[test]
  #[available_gas(3000000000000000)]
  fn integration() {
    // let player_one = starknet::contract_address_const::<0x01>();
    // let player_two = starknet::contract_address_const::<0x02>();

  }
}
